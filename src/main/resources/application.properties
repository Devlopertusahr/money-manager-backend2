# Application Name
spring.application.name=moneymanager2

# Database Configuration for Render (Render will provide these)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://shinkansen.proxy.rlwy.net:27756/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${JDBC_DATABASE_USERNAME:root}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:lhtafmIfGIDTTCxwnJvlAmOhJlEWskwJ}

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.servlet.context-path=/api/v1.0
server.port=${PORT:8080}

# Activation URL - Render ke URL ke liye dynamic
app.activation.url=${APP_URL:http://localhost:8080}/api/v1.0/activate

# SMTP Configuration for Brevo
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
spring.mail.username=932fcc001@smtp-brevo.com
spring.mail.password=AXDqYs5U9QVw2ZCz
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.from=tushartiwari30303@gmail.com

# JWT Secret
jwt.secret=2b8e6b8e-5e8j-7m1l-9c2l-8f7e2d4b

# Frontend URL
money.manager.frontend.url=https://resplendent-druid-d5491d.netlify.app
